apiVersion: v1
kind: Service
metadata:
  name: video-processor-worker
  labels:
    app: video-processor-worker
spec:
  type: ClusterIP
  selector:
    app: video-processor-worker
  ports:
    - port: 9090
      targetPort: health
      protocol: TCP
      name: http
